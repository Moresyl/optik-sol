# optik-core

The dependency-free instrumentation kernel behind [Optik](https://github.com/Moresyl/optik). It captures console output, errors, network activity, storage state, and system information without touching the DOM.

Most users should install [`optik-debug`](https://www.npmjs.com/package/optik-debug), which includes the complete in-page console.

## License

MIT
